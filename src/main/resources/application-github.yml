# need to change to your configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fafood?useSSL=false&useUnicode=true&characterEncoding=UTF-8&autoReconnect=TRUE
    username: localhost
    password: localhost
  redis:
    password: localhost
    host: localhost
    port: 6351
    database: 0